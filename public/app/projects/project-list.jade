.container
  table.table.table-hover.table-striped.table-condensed
    thead
      tr
        th Title
        th Publish Date
      tbody
        tr(ng-repeat="project in projects")
          td {{project.title}}
          td {{project.published | date}}
